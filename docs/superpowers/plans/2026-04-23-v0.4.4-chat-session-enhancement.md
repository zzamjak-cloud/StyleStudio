# v0.4.4 채팅 세션 고도화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** StyleStudio 채팅 세션에 기획 문서(PDF/MD) 기반 대화를 추가하고, 세션 생성 UX·저장 폴더 관리·공통 UI 레이아웃을 단순화한다.

**Architecture:** 기존 인프라(`DocumentManager`, `fileParser`, `pdfjs-dist`)를 채팅 세션에 통합한다. PDF 이미지 추출은 `pdfjs-dist`의 `getOperatorList` + `page.objs.get`으로 내장 이미지를 base64로 변환해 AI 요청에 포함한다. 저장 경로는 `~/Downloads/AI_Gen/` 고정 상수로 단일화하고, 세션별 `autoSavePath`는 deprecated 처리한다.

**Tech Stack:** React 19, TypeScript 5.8, Vite 7, Tailwind CSS 3, Tauri 2, lucide-react, pdfjs-dist 5.4, @tauri-apps/plugin-opener

**Spec:** [docs/superpowers/specs/2026-04-23-v0.4.4-chat-session-enhancement-design.md](../specs/2026-04-23-v0.4.4-chat-session-enhancement-design.md)

**Testing note:** 이 프로젝트에는 단위 테스트 프레임워크가 설치되어 있지 않다. 각 task의 검증은 **TypeScript 타입 체크(`npm run build`)** + **dev 서버 수동 시나리오 검증**으로 수행한다. 각 task 끝에 검증 절차와 기대 동작을 명시한다.

---

## Task 0: 사전 준비 및 경로 상수 정의

**Files:**
- Create: `src/lib/config/paths.ts`

**Purpose:** `~/Downloads/AI_Gen/` 관련 경로를 한 곳에서 관리하기 위한 유틸/상수 모듈 신설. 이후 모든 task에서 이 모듈을 사용한다.

- [ ] **Step 0-1: `src/lib/config/paths.ts` 작성**

```ts
import { downloadDir, join } from '@tauri-apps/api/path';
import { exists, mkdir } from '@tauri-apps/plugin-fs';

// v0.4.4: 모든 생성 결과물의 루트 폴더 (~/Downloads/AI_Gen/)
export const AI_GEN_ROOT_SEGMENT = 'AI_Gen';
// 세션 메타데이터 저장 폴더
export const SESSIONS_SEGMENT = 'Sessions';

/** ~/Downloads/AI_Gen/ 절대 경로를 돌려준다. 없으면 생성한다. */
export async function getAiGenRoot(): Promise<string> {
  const downloads = await downloadDir();
  const root = await join(downloads, AI_GEN_ROOT_SEGMENT);
  if (!(await exists(root))) {
    await mkdir(root, { recursive: true });
  }
  return root;
}

/** ~/Downloads/AI_Gen/Sessions/ 절대 경로를 돌려준다. 없으면 생성한다. */
export async function getSessionsRoot(): Promise<string> {
  const root = await getAiGenRoot();
  const sessions = await join(root, SESSIONS_SEGMENT);
  if (!(await exists(sessions))) {
    await mkdir(sessions, { recursive: true });
  }
  return sessions;
}

/**
 * 특정 세션 이름에 대한 생성 이미지 저장 폴더 경로를 돌려준다.
 * (~/Downloads/AI_Gen/{sanitized session name}/)
 */
export async function getSessionImageFolder(sessionName: string): Promise<string> {
  const root = await getAiGenRoot();
  const safe = sanitizeFolderName(sessionName);
  const folder = await join(root, safe);
  if (!(await exists(folder))) {
    await mkdir(folder, { recursive: true });
  }
  return folder;
}

/** Windows/macOS 공통 금지 문자 치환 */
export function sanitizeFolderName(name: string): string {
  return name.replace(/[\\/:*?"<>|]/g, '_').trim() || 'untitled';
}
```

- [ ] **Step 0-2: 타입 체크**

Run: `cd /Users/woody/Desktop/AI/StyleStudio && npm run build`
Expected: 에러 없이 완료.

- [ ] **Step 0-3: 커밋**

```bash
git add src/lib/config/paths.ts
git commit -m "feat(paths): AI_Gen 루트/세션 폴더 경로 상수 및 유틸 추가"
```

---

## Task 1: 타입 확장 — `ChatMessage.documents`, `ReferenceDocument.extractedImages`

**Files:**
- Modify: `src/types/chat.ts`
- Modify: `src/types/referenceDocument.ts`

**Purpose:** 채팅 메시지에 문서 첨부 정보를 보관하고, PDF 내장 이미지를 문서에 달아두기 위한 필드를 추가한다.

- [ ] **Step 1-1: `src/types/chat.ts` 수정**

파일 상단 import 구문 아래에 `ReferenceDocument` import 추가:

```ts
import { ReferenceDocument } from './referenceDocument';
```

`ChatMessage` 인터페이스에 `documents` 추가:

```ts
export interface ChatMessage {
  id: string;
  role: 'user' | 'assistant' | 'summary';
  content: string;
  images?: string[];
  documents?: ReferenceDocument[]; // v0.4.4: 첨부 문서
  timestamp: string;
  isGeneratedImage?: boolean;
  tokenCount?: number;
  imageSignatures?: string[];
}
```

`estimateTokenCount` 함수에 문서 텍스트 토큰 합산을 추가:

```ts
export function estimateTokenCount(message: ChatMessage): number {
  let tokens = 0;
  if (message.content) {
    tokens += Math.ceil(message.content.length / 2);
  }
  if (message.images) {
    tokens += message.images.length * 258;
  }
  if (message.documents) {
    for (const doc of message.documents) {
      if (doc.content) tokens += Math.ceil(doc.content.length / 2);
      if (doc.extractedImages) tokens += doc.extractedImages.length * 258;
    }
  }
  return tokens;
}
```

- [ ] **Step 1-2: `src/types/referenceDocument.ts` 수정**

```ts
export interface ReferenceDocument {
  id: string;
  fileName: string;
  filePath: string;
  fileType: string;
  content: string;
  summary?: string;
  metadata?: {
    pageCount?: number;
    sheetCount?: number;
    lineCount?: number;
    characterCount?: number;
    extractedImageCount?: number; // v0.4.4
  };
  extractedImages?: string[]; // v0.4.4: PDF 내장 이미지 (data URL)
  createdAt: number;
  updatedAt: number;
}
```

- [ ] **Step 1-3: 타입 체크**

Run: `cd /Users/woody/Desktop/AI/StyleStudio && npm run build`
Expected: 에러 없이 완료. (기존 사용처가 새 필드를 optional로 가지므로 영향 없음)

- [ ] **Step 1-4: 커밋**

```bash
git add src/types/chat.ts src/types/referenceDocument.ts
git commit -m "feat(types): ChatMessage.documents 및 ReferenceDocument.extractedImages 필드 추가"
```

---

## Task 2: PDF 이미지 추출 로직 구현

**Files:**
- Modify: `src/lib/utils/fileParser.ts` (기존 `parsePdf` 함수 확장)

**Purpose:** PDF 파싱 시 내장 이미지를 추출해 `ReferenceDocument.extractedImages`에 저장한다.

- [ ] **Step 2-1: `fileParser.ts` PDF 파싱 부분 확인**

현재 `parsePdf`의 실제 라인 위치 확인:

Run: `grep -n "parsePdf\|getDocument\|getPage" /Users/woody/Desktop/AI/StyleStudio/src/lib/utils/fileParser.ts | head -20`
Expected: `parsePdf` 함수 정의 라인이 출력됨.

- [ ] **Step 2-2: PDF 이미지 추출 헬퍼 함수 추가**

`fileParser.ts` 상단 import 아래에 다음 헬퍼를 추가한다 (파일에 아직 없는 경우):

```ts
import type { PDFPageProxy } from 'pdfjs-dist';

/**
 * 주어진 PDF 페이지에서 내장 이미지를 추출해 PNG data URL 배열로 반환.
 * 실패 시 빈 배열을 반환한다 (텍스트 파싱은 계속 진행).
 */
async function extractImagesFromPdfPage(page: PDFPageProxy): Promise<string[]> {
  const out: string[] = [];
  try {
    const ops = await page.getOperatorList();
    const OPS = (pdfjsLib as any).OPS;
    const targetOps = [OPS.paintImageXObject, OPS.paintInlineImageXObject, OPS.paintJpegXObject];

    for (let i = 0; i < ops.fnArray.length; i++) {
      if (!targetOps.includes(ops.fnArray[i])) continue;
      const name = ops.argsArray[i]?.[0];
      if (!name) continue;

      // pdf.js는 이미지 오브젝트를 resolve 후 get 으로 획득
      const img: any = await new Promise((resolve) => {
        try {
          page.objs.get(name, resolve);
        } catch {
          resolve(null);
        }
      });
      if (!img || !img.width || !img.height) continue;

      const dataUrl = await renderPdfImageToDataUrl(img);
      if (dataUrl) out.push(dataUrl);

      // 메모리 보호: 페이지당 최대 10장
      if (out.length >= 10) break;
    }
  } catch (error) {
    console.warn('[pdf] 이미지 추출 실패, 건너뜀:', error);
  }
  return out;
}

/** pdf.js 이미지 오브젝트를 OffscreenCanvas로 그려 PNG data URL로 변환 */
async function renderPdfImageToDataUrl(img: {
  width: number;
  height: number;
  data?: Uint8ClampedArray | Uint8Array;
  kind?: number; // 1=Grayscale, 2=RGB, 3=RGBA
  bitmap?: ImageBitmap;
}): Promise<string | null> {
  try {
    const canvas =
      typeof OffscreenCanvas !== 'undefined'
        ? new OffscreenCanvas(img.width, img.height)
        : Object.assign(document.createElement('canvas'), { width: img.width, height: img.height });
    const ctx = (canvas as any).getContext('2d');
    if (!ctx) return null;

    if (img.bitmap) {
      ctx.drawImage(img.bitmap, 0, 0);
    } else if (img.data) {
      const rgba = toRgbaBuffer(img.data, img.kind ?? 2, img.width, img.height);
      const imageData = new ImageData(rgba, img.width, img.height);
      ctx.putImageData(imageData, 0, 0);
    } else {
      return null;
    }

    if (canvas instanceof OffscreenCanvas) {
      const blob = await canvas.convertToBlob({ type: 'image/png' });
      return await blobToDataUrl(blob);
    } else {
      return (canvas as HTMLCanvasElement).toDataURL('image/png');
    }
  } catch (error) {
    console.warn('[pdf] 이미지 변환 실패:', error);
    return null;
  }
}

function toRgbaBuffer(
  src: Uint8ClampedArray | Uint8Array,
  kind: number,
  width: number,
  height: number
): Uint8ClampedArray {
  const total = width * height;
  const out = new Uint8ClampedArray(total * 4);
  if (kind === 1) {
    // Grayscale
    for (let i = 0; i < total; i++) {
      const v = src[i];
      out[i * 4] = v;
      out[i * 4 + 1] = v;
      out[i * 4 + 2] = v;
      out[i * 4 + 3] = 255;
    }
  } else if (kind === 3) {
    // RGBA
    out.set(src);
  } else {
    // RGB (kind=2 or unknown)
    for (let i = 0; i < total; i++) {
      out[i * 4] = src[i * 3];
      out[i * 4 + 1] = src[i * 3 + 1];
      out[i * 4 + 2] = src[i * 3 + 2];
      out[i * 4 + 3] = 255;
    }
  }
  return out;
}

function blobToDataUrl(blob: Blob): Promise<string> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result as string);
    reader.onerror = () => reject(reader.error);
    reader.readAsDataURL(blob);
  });
}
```

- [ ] **Step 2-3: 기존 `parsePdf` 함수에 이미지 추출 연동**

`parsePdf`에서 각 페이지 텍스트 추출 루프 내 또는 반환 직전에 다음을 추가:

```ts
// 기존 텍스트 추출 루프 안에서 page를 이미 획득하고 있다면 같은 루프에서 이미지도 수집
const extractedImages: string[] = [];
// (루프 내) const pageImages = await extractImagesFromPdfPage(page);
//            extractedImages.push(...pageImages);

// 반환 객체에 필드 추가
return {
  content,
  metadata: {
    pageCount: numPages,
    characterCount: content.length,
    extractedImageCount: extractedImages.length,
  },
  extractedImages,
};
```

**주의:** `parsePdf`의 반환 타입인 `ParsedFileContent`에 `extractedImages?: string[]` 필드를 아직 추가하지 않았다면 다음 단계에서 추가한다.

- [ ] **Step 2-4: `ParsedFileContent` 타입 확장**

`fileParser.ts` 상단 `ParsedFileContent` 인터페이스에 필드 추가:

```ts
export interface ParsedFileContent {
  content: string;
  metadata?: { pageCount?: number; sheetCount?: number; lineCount?: number; characterCount?: number; extractedImageCount?: number };
  extractedImages?: string[]; // v0.4.4: PDF 내장 이미지 (data URL)
}
```

`parseFile` 등 `ParsedFileContent`를 반환하는 모든 경로가 새 optional 필드를 허용하는지 확인.

- [ ] **Step 2-5: `ReferenceDocument` 생성부 업데이트**

`DocumentManager.tsx`에서 `parseFile` 결과로 `ReferenceDocument`를 생성하는 코드를 찾아(`onAdd` 호출 근처) `extractedImages`를 복사:

```ts
const doc: ReferenceDocument = {
  // ... 기존 필드
  extractedImages: parsed.extractedImages,
  metadata: { ...parsed.metadata },
};
```

실제 수정 위치: `grep -n "onAdd\|parseFile\|ReferenceDocument" /Users/woody/Desktop/AI/StyleStudio/src/components/generator/DocumentManager.tsx`

- [ ] **Step 2-6: 타입 체크 + dev 수동 검증**

Run: `npm run build`
Expected: 에러 없이 완료.

수동 검증 (선택): `npm run tauri dev` 실행 후 UI 세션에서 이미지가 포함된 PDF를 첨부 → 개발자 도구 콘솔에서 `referenceDocuments[0].extractedImages`의 길이 확인.

- [ ] **Step 2-7: 커밋**

```bash
git add src/lib/utils/fileParser.ts src/components/generator/DocumentManager.tsx
git commit -m "feat(pdf): PDF 내장 이미지 추출 로직 추가 (pdfjs-dist OPS 파싱)"
```

---

## Task 3: 경로 상수화 — 모든 `downloadDir()` 호출을 `getAiGenRoot()`로 대체

**Files:**
- Modify: `src/components/chat/ChatPanel.tsx:120-135` (근처 `downloadDir()` 블록)
- Modify: `src/components/generator/ImageGeneratorPanel.tsx:715-730`, `:862-875`
- Modify: `src/components/concept/ConceptPanel.tsx:45-55`

**Purpose:** 생성 이미지 저장 시 항상 `~/Downloads/AI_Gen/{세션명}/` 를 사용한다.

- [ ] **Step 3-1: ChatPanel 저장 경로 교체**

기존:
```ts
const downloadPath = await downloadDir();
const folder = await join(downloadPath, 'StyleStudio', /* ... */);
```

변경 (실제 코드 구조에 맞춰 `join` 사용):
```ts
import { getSessionImageFolder } from '../../lib/config/paths';
// ...
const folder = await getSessionImageFolder(session.name);
```

`downloadDir` / `join` import가 더 이상 필요 없으면 제거.

- [ ] **Step 3-2: ImageGeneratorPanel 저장 경로 교체 (두 군데)**

715-730행, 862-875행 동일 패턴 적용.

- [ ] **Step 3-3: ConceptPanel 저장 경로 교체**

45-55행 동일 패턴 적용.

- [ ] **Step 3-4: `Session.autoSavePath` deprecated 처리**

`src/types/session.ts` 20행 주석 수정:

```ts
/** @deprecated v0.4.4 이후 사용 안 함. getSessionImageFolder() 사용 */
autoSavePath?: string;
```

`autoSavePath`를 **쓰기**하는 코드는 모두 제거한다:

Run: `grep -rn "autoSavePath\s*=" /Users/woody/Desktop/AI/StyleStudio/src --include="*.ts" --include="*.tsx"`

각 할당 지점을 제거하거나 무시 처리한다. 읽기만 하는 코드는 `getSessionImageFolder()` 호출로 교체.

- [ ] **Step 3-5: 타입 체크**

Run: `npm run build`
Expected: 에러 없이 완료.

- [ ] **Step 3-6: 커밋**

```bash
git add -A
git commit -m "refactor(paths): 생성 이미지 저장 경로를 ~/Downloads/AI_Gen/{세션명}/ 으로 고정"
```

---

## Task 4: 새 세션 모달 단순화 — 이름 입력 제거 + 자동 네이밍 + 아이콘 변경

**Files:**
- Modify: `src/components/common/NewSessionModal.tsx`

**Purpose:** 세션 이름 입력 필드를 제거하고 자동으로 `{label}_{N}` 형식으로 지정한다. BASIC(채팅) 아이콘을 STYLE과 구분되게 변경한다.

- [ ] **Step 4-1: NewSessionModal props 확인**

Run: `grep -n "interface\|props\|export function" /Users/woody/Desktop/AI/StyleStudio/src/components/common/NewSessionModal.tsx | head -20`

기존 props에 `existingSessions: Session[]` 또는 유사한 배열이 있는지 확인. 없으면 호출자(`App.tsx` 또는 `SessionList.tsx`)에서 전달하도록 추가.

- [ ] **Step 4-2: 자동 네이밍 유틸 추가**

`NewSessionModal.tsx` 상단에 헬퍼 함수 추가:

```ts
import { SESSION_CONFIG } from '../../lib/config/sessionConfig';
import { Session, SessionType } from '../../types/session';

function generateSessionName(type: SessionType, existingSessions: Session[]): string {
  const label = SESSION_CONFIG[type].label;
  const sameType = existingSessions.filter((s) => s.type === type);
  // 타입별 전체 카운트 기준 — 삭제된 번호 재사용 안 함 (최대값 유지)
  let maxIndex = 0;
  const pattern = new RegExp(`^${label}_(\\d+)$`);
  for (const s of sameType) {
    const m = s.name.match(pattern);
    if (m) maxIndex = Math.max(maxIndex, parseInt(m[1], 10));
  }
  return `${label}_${Math.max(maxIndex, sameType.length) + 1}`;
}
```

- [ ] **Step 4-3: 이름 입력 필드 제거 및 자동 네이밍 적용**

63-71행의 `<input>` 블록 전체 제거. `useState` 로 관리하던 `name` 상태도 제거.

"시작하기" 버튼(307-312행) onClick을 다음과 같이 수정:

```tsx
<button
  onClick={() => {
    const name = generateSessionName(selectedType, existingSessions);
    onCreate({ type: selectedType, name });
  }}
  disabled={!selectedType}
  className="..."
>
  시작하기
</button>
```

`onCreate` prop 시그니처가 `(data: { type, name }) => void` 형태가 되도록 호출자(App.tsx/SessionList.tsx)도 함께 수정한다.

- [ ] **Step 4-4: BASIC 세션 아이콘 변경**

파일 상단 `lucide-react` import 에 `MessageSquare` 또는 `MessagesSquare` 추가:

```tsx
import { MessageSquare, Palette, /* ... */ } from 'lucide-react';
```

`BASIC` 카드의 아이콘 렌더링 부분을 `MessageSquare`로 변경 (기존에 `MessageCircle`이었다면 교체). `STYLE`은 `Palette` 유지 확인.

실제 파일에서 어떤 아이콘을 쓰는지 확인:
Run: `grep -n "BASIC\|STYLE\|icon:" /Users/woody/Desktop/AI/StyleStudio/src/components/common/NewSessionModal.tsx | head -20`

- [ ] **Step 4-5: 타입 체크**

Run: `npm run build`
Expected: 에러 없이 완료.

- [ ] **Step 4-6: dev 수동 검증**

Run: `npm run tauri dev` (이미 실행 중이면 생략)
- 새 세션 버튼 클릭 → 이름 필드 사라졌는지, 타입 선택만으로 "시작하기" 활성화 되는지.
- BASIC 생성 후 이름이 `기본(채팅)_1` (또는 실제 label)로 자동 지정되는지.
- BASIC 아이콘이 STYLE과 시각적으로 구분되는지.

- [ ] **Step 4-7: 커밋**

```bash
git add src/components/common/NewSessionModal.tsx src/App.tsx src/components/Sidebar*.tsx 2>/dev/null
git commit -m "feat(ui): 새 세션 모달에서 이름 입력 제거 및 타입별 자동 네이밍 적용"
```

(호출자 파일명이 다르면 실제 수정된 파일만 add)

---

## Task 5: BASIC 채팅 세션에 DocumentManager 통합

**Files:**
- Modify: `src/components/chat/ChatPanel.tsx`
- Modify: `src/components/chat/ChatInput.tsx` (필요 시)

**Purpose:** 채팅 세션에서도 문서를 첨부할 수 있게 한다. 첨부된 문서는 다음 사용자 메시지에 포함되어 전송된다.

- [ ] **Step 5-1: ChatPanel 상태 추가**

`ChatPanel.tsx` 상단 state 블록에 추가:

```tsx
const [chatDocuments, setChatDocuments] = useState<ReferenceDocument[]>([]);
```

- [ ] **Step 5-2: DocumentManager 렌더링**

메시지 입력 영역 위(또는 적절한 사이드 영역)에 `DocumentManager`를 렌더링:

```tsx
<DocumentManager
  documents={chatDocuments}
  apiKey={apiKey}
  onAdd={(doc) => setChatDocuments((prev) => [...prev, doc])}
  onDelete={(id) => setChatDocuments((prev) => prev.filter((d) => d.id !== id))}
/>
```

- [ ] **Step 5-3: 메시지 전송 시 문서 첨부**

기존 `handleSend` (또는 사용자 메시지 구성 지점)에서 `ChatMessage`에 `documents`를 포함:

```tsx
const userMessage: ChatMessage = {
  id: crypto.randomUUID(),
  role: 'user',
  content: input,
  images: attachedImages,
  documents: chatDocuments.length > 0 ? [...chatDocuments] : undefined,
  timestamp: new Date().toISOString(),
};
```

- [ ] **Step 5-4: AI 요청에 문서 컨텍스트 + 이미지 주입**

`useGeminiImageGenerator` 또는 채팅 전송 서비스 호출 부분에서:

```ts
// 문서 텍스트를 시스템 프롬프트에 주입
const documentContext = chatDocuments
  .map((d) => `[문서: ${d.fileName}]\n${d.content}`)
  .join('\n\n');

// 문서 내장 이미지를 참조 이미지 배열에 합침
const allReferenceImages = [
  ...attachedImages,
  ...chatDocuments.flatMap((d) => d.extractedImages ?? []),
];

// 호출부에 documentContext, allReferenceImages 전달
```

실제 호출 함수 시그니처 확인:
Run: `grep -n "generateImage\|callGemini\|sendMessage" /Users/woody/Desktop/AI/StyleStudio/src/hooks/api/useGeminiImageGenerator.ts | head`

필요 시 `useGeminiImageGenerator`의 옵션 타입에 `systemContext?: string` 파라미터를 추가.

- [ ] **Step 5-5: 문서-only 케이스 프롬프트 템플릿 적용**

이미지 없이 문서만 첨부된 경우, 사용자가 빈 프롬프트로 전송하지 않도록 guard하거나 자동 템플릿을 주입:

```ts
const isDocumentOnly =
  attachedImages.length === 0 && chatDocuments.length > 0;

const effectivePrompt = isDocumentOnly && !input.trim()
  ? '첨부된 기획 문서를 바탕으로 완성도 높은 모바일 캐주얼 게임의 인게임 이미지를 생성해주세요.'
  : input;
```

- [ ] **Step 5-6: 전송 후 상태 초기화**

전송 성공 후 `setChatDocuments([])` 로 초기화.

- [ ] **Step 5-7: 타입 체크 + dev 수동 검증**

Run: `npm run build`
Expected: 에러 없이 완료.

수동 검증:
- BASIC 세션에서 PDF만 첨부 → 빈 프롬프트로 전송 → 이미지 생성되는지.
- PDF + 이미지 동시 첨부 → 생성 정상 동작하는지.
- 전송 후 문서 첨부 상태 초기화 확인.

- [ ] **Step 5-8: 커밋**

```bash
git add src/components/chat/*.tsx src/hooks/api/useGeminiImageGenerator.ts
git commit -m "feat(chat): BASIC 세션에 문서 첨부 및 문서 컨텍스트 기반 이미지 생성 추가"
```

---

## Task 6: 이미지 분석 화면 상단 버튼 sticky 처리

**Files:**
- Modify: `src/components/analysis/AnalysisPanel.tsx:170-230`

**Purpose:** "분석 강화", "저장", "생성" 버튼을 상단 고정하고, 프롬프트 영역만 스크롤 되게 한다.

- [ ] **Step 6-1: 현재 구조 파악**

Run: `sed -n '160,240p' /Users/woody/Desktop/AI/StyleStudio/src/components/analysis/AnalysisPanel.tsx`
(Read 도구로 해도 됨)

- [ ] **Step 6-2: 레이아웃 재구성**

버튼 그룹 div에 `sticky top-0 z-10 bg-white border-b` 추가:

```tsx
<div className="sticky top-0 z-10 bg-white/95 backdrop-blur-sm border-b border-gray-200 px-4 py-2 flex items-center justify-between">
  {/* 기존 "분석 강화" "저장" "생성" 버튼들 */}
</div>

<div className="overflow-y-auto">
  {/* 기존 분석 프롬프트/결과 */}
</div>
```

외곽 컨테이너는 `flex flex-col h-full` 형태가 유지되어야 함. 필요 시 wrapper div 추가.

- [ ] **Step 6-3: 헤더 패딩 축소 (Task 9 선행 일부)**

버튼 그룹 sticky 컨테이너의 세로 패딩을 `py-2` 또는 `py-1.5`로 지정해 공간을 아낀다.

- [ ] **Step 6-4: 타입 체크 + dev 수동 검증**

Run: `npm run build`
수동 검증: 분석 결과가 긴 경우 스크롤해도 버튼이 상단에 남아 있는지 확인.

- [ ] **Step 6-5: 커밋**

```bash
git add src/components/analysis/AnalysisPanel.tsx
git commit -m "feat(ui): 이미지 분석 패널의 분석/저장/생성 버튼 상단 고정"
```

---

## Task 7: 저장 폴더 버튼 동작 변경 (탐색기 열기) + 세션별 폴더 지정 제거

**Files:**
- Modify: `src/components/generator/ImageGeneratorPanel.tsx:1140-1170`
- Modify: `src/components/common/SettingsModal.tsx:130-180`

**Purpose:** "저장 폴더" 버튼이 `~/Downloads/AI_Gen/` 를 OS 파일 탐색기에서 연다. 세션별 / 설정 팝업의 저장 폴더 지정 UI를 제거한다.

- [ ] **Step 7-1: opener import**

`ImageGeneratorPanel.tsx` 상단에 추가:

```tsx
import { openPath } from '@tauri-apps/plugin-opener';
import { getAiGenRoot } from '../../lib/config/paths';
```

- [ ] **Step 7-2: "저장 폴더" 버튼 onClick 교체**

1150-1155행 부근 기존 "저장 폴더 선택" 다이얼로그 호출을 다음으로 교체:

```tsx
<button
  onClick={async () => {
    try {
      const root = await getAiGenRoot();
      await openPath(root);
    } catch (e) {
      console.error('폴더 열기 실패:', e);
      alert('저장 폴더를 여는 데 실패했습니다.');
    }
  }}
  className="..."
  title="생성 이미지가 저장된 폴더를 탐색기에서 엽니다"
>
  <FolderOpen className="w-4 h-4" />
  저장 폴더
</button>
```

- [ ] **Step 7-3: 세션별 저장 폴더 지정 UI 제거**

`ImageGeneratorPanel.tsx` 내 `autoSavePath` 관련 input / 다이얼로그 트리거 UI 전체 삭제.

Run: `grep -n "autoSavePath\|저장 폴더 선택\|setAutoSavePath" /Users/woody/Desktop/AI/StyleStudio/src/components/generator/ImageGeneratorPanel.tsx`

위 grep에 나오는 모든 관련 블록을 제거.

- [ ] **Step 7-4: 설정 팝업의 기본 저장 폴더 UI 제거**

`SettingsModal.tsx:140-168` 폴더 선택/초기화 버튼 블록을 다음과 같이 안내 텍스트로 교체:

```tsx
<div className="flex items-center justify-between p-3 bg-gray-50 rounded-lg border border-gray-200">
  <div>
    <div className="text-sm font-medium text-gray-700">생성 이미지 저장 위치</div>
    <div className="text-xs text-gray-500 mt-0.5">
      ~/Downloads/AI_Gen/ 에 세션 이름으로 자동 저장됩니다
    </div>
  </div>
  <button
    onClick={async () => {
      const root = await getAiGenRoot();
      await openPath(root);
    }}
    className="px-3 py-1.5 text-sm rounded-md bg-indigo-600 text-white hover:bg-indigo-700 flex items-center gap-1.5"
  >
    <FolderOpen className="w-4 h-4" />
    폴더 열기
  </button>
</div>
```

`SettingsModal.tsx` 상단에 `openPath`, `getAiGenRoot`, `FolderOpen` import 추가.

- [ ] **Step 7-5: Tauri capabilities 확인**

Run: `grep -n "opener" /Users/woody/Desktop/AI/StyleStudio/src-tauri/capabilities/default.json`

`opener:allow-open-path` 또는 `opener:default` 권한이 있는지 확인. 없으면 추가:

```json
{
  "permissions": [
    // ...
    "opener:default",
    "opener:allow-open-path"
  ]
}
```

- [ ] **Step 7-6: 타입 체크 + dev 수동 검증**

Run: `npm run build`
수동: 저장 폴더 버튼 클릭 시 Finder/탐색기에서 `~/Downloads/AI_Gen/` 가 열리는지 확인.

- [ ] **Step 7-7: 커밋**

```bash
git add src/components/generator/ImageGeneratorPanel.tsx src/components/common/SettingsModal.tsx src-tauri/capabilities/default.json
git commit -m "feat(ui): 저장 폴더 버튼을 탐색기 열기로 변경 + 세션별 저장 경로 지정 UI 제거"
```

---

## Task 8: 그리드 버튼 1라인 배치 + 공통 헤더 축소

**Files:**
- Modify: `src/components/generator/GeneratorSettings.tsx:212-260`
- Modify: `src/components/generator/ImageGeneratorPanel.tsx` (헤더 영역)
- Modify: `src/components/analysis/AnalysisPanel.tsx` (헤더 영역 — Task 6에서 일부 완료)

**Purpose:** 그리드 개수 선택 버튼 4개를 한 줄에 배치하고, 상단 헤더 패딩을 축소해 콘텐츠 공간을 확보한다.

- [ ] **Step 8-1: 그리드 버튼 컨테이너 수정**

`GeneratorSettings.tsx:212`:

```tsx
// Before
<div className="grid grid-cols-2 gap-3">

// After
<div className="grid grid-cols-4 gap-2">
```

4개 버튼 각각의 내부 패딩/폰트를 축소:

```tsx
<button
  onClick={() => onPixelArtGridChange('1x1')}
  className={`p-2 rounded-md text-xs font-medium border-2 transition-all ${getGridButtonStyle(
    sessionType,
    pixelArtGrid === '1x1'
  )}`}
>
  <div className="font-bold">1x1</div>
  <div className="text-[10px] opacity-75">단일</div>
</button>
```

(2x2, 3x3, 4x4도 동일하게)

- [ ] **Step 8-2: ImageGeneratorPanel 헤더 축소**

Run: `grep -n "py-4\|py-3\|h-14\|h-16" /Users/woody/Desktop/AI/StyleStudio/src/components/generator/ImageGeneratorPanel.tsx | head -10`

상단 헤더 영역의 세로 패딩/높이를 한 단계 축소 (예: `py-4` → `py-2`, `h-16` → `h-12`).

- [ ] **Step 8-3: AnalysisPanel 헤더 축소**

Task 6에서 sticky 헤더 패딩을 이미 `py-2`로 지정했으면 추가 작업 없음. 그 외 나머지 상단 타이틀 영역이 있으면 동일 기준으로 축소.

- [ ] **Step 8-4: 타입 체크 + dev 수동 검증**

Run: `npm run build`
수동: 모든 세션 타입에서 그리드 버튼이 한 줄에 4개 표시되는지, 버튼이 잘리거나 텍스트가 2줄로 깨지지 않는지 확인.

- [ ] **Step 8-5: 커밋**

```bash
git add src/components/generator/GeneratorSettings.tsx src/components/generator/ImageGeneratorPanel.tsx src/components/analysis/AnalysisPanel.tsx
git commit -m "feat(ui): 그리드 버튼 1라인 배치 및 공통 헤더 높이 축소"
```

---

## Task 9: 세션 메타데이터 저장 경로 `Sessions/` 이관 (선택)

**Files:**
- Modify: 세션 저장을 담당하는 서비스/훅 (예: IndexedDB 외 파일 저장 경로)

**Purpose:** 계획 문서의 "`download/AI_Gen/Session` 폴더 생성" 요구사항 충족. 현재 세션은 IndexedDB(idb)에 저장되고 있어 파일 저장은 **백업/내보내기 경로**에 해당할 수 있음.

- [ ] **Step 9-1: 현재 세션 저장 방식 확인**

Run: `grep -rn "indexedDB\|idb\|session" /Users/woody/Desktop/AI/StyleStudio/src/lib/services --include="*.ts" | head -10`

**분기:**
- 세션을 파일로 저장하지 않는 경우(IndexedDB만 사용) → Task 9 전체를 스킵하고 아래 Step 9-5로 점프해 커밋 없이 종료.
- 파일 기반 저장이 있는 경우 → Step 9-2 진행.

- [ ] **Step 9-2: 저장 경로 교체**

기존 세션 파일 저장 코드의 경로를 `getSessionsRoot()` 사용하도록 수정:

```ts
import { getSessionsRoot } from '../../lib/config/paths';
// ...
const sessionsFolder = await getSessionsRoot();
const sessionFile = await join(sessionsFolder, `${session.id}.json`);
await writeFile(sessionFile, new TextEncoder().encode(JSON.stringify(session)));
```

- [ ] **Step 9-3: 첫 실행 시 기존 경로에서 이관**

(선택) 앱 초기화 시 구 경로 → 신 경로로 세션 파일 이동. 실패 시 무시.

- [ ] **Step 9-4: 타입 체크 + dev 수동 검증**

Run: `npm run build`
수동: `~/Downloads/AI_Gen/Sessions/` 폴더가 생성되었는지 Finder에서 확인.

- [ ] **Step 9-5: 커밋 (적용된 경우에만)**

```bash
git add -A
git commit -m "feat(sessions): 세션 메타데이터 저장 폴더를 ~/Downloads/AI_Gen/Sessions/ 로 이관"
```

---

## Task 10: 종료 검증 및 버전 bump

**Files:**
- Modify: `package.json` (version)
- Modify: `src-tauri/Cargo.toml` (version)
- Modify: `src-tauri/tauri.conf.json` (version)

- [ ] **Step 10-1: 전체 빌드 통과 확인**

Run: `cd /Users/woody/Desktop/AI/StyleStudio && npm run build 2>&1 | tail -20`
Expected: `built in XX.XXs` 로 종료, TypeScript 에러 없음.

- [ ] **Step 10-2: dev 서버 전체 시나리오 수동 검증**

Run: `npm run tauri dev` (아직 실행 중이면 생략)

검증 체크리스트:
- [ ] 새 세션 모달에 이름 필드 없음
- [ ] BASIC 세션 자동 이름 `기본(채팅)_N` 정상 증가
- [ ] BASIC 아이콘이 STYLE과 구분됨
- [ ] 분석 패널 버튼 sticky 확인
- [ ] 저장 폴더 버튼 → `~/Downloads/AI_Gen/` 탐색기 오픈
- [ ] 설정 팝업에 기본 저장 폴더 선택 UI 없음
- [ ] 채팅 세션에 PDF 첨부 후 전송 성공
- [ ] PDF 내장 이미지가 AI 요청에 포함되어 결과에 영향 (콘솔 로그 확인)
- [ ] 그리드 버튼 모든 세션에서 1라인 4개 배치
- [ ] 생성된 이미지가 `~/Downloads/AI_Gen/{세션명}/` 하위에 저장됨

- [ ] **Step 10-3: 버전 bump**

세 파일을 `0.4.3` → `0.4.4`로 변경:

```bash
sed -i.bak 's/"version": "0.4.3"/"version": "0.4.4"/' /Users/woody/Desktop/AI/StyleStudio/package.json
sed -i.bak 's/^version = "0.4.3"$/version = "0.4.4"/' /Users/woody/Desktop/AI/StyleStudio/src-tauri/Cargo.toml
sed -i.bak 's/"version": "0.4.3"/"version": "0.4.4"/' /Users/woody/Desktop/AI/StyleStudio/src-tauri/tauri.conf.json
rm /Users/woody/Desktop/AI/StyleStudio/package.json.bak /Users/woody/Desktop/AI/StyleStudio/src-tauri/Cargo.toml.bak /Users/woody/Desktop/AI/StyleStudio/src-tauri/tauri.conf.json.bak
```

- [ ] **Step 10-4: Cargo.lock 갱신 확인**

Run: `cd /Users/woody/Desktop/AI/StyleStudio/src-tauri && cargo check --quiet 2>&1 | tail -5`
Expected: 에러 없음. Cargo.lock 버전 필드가 0.4.4로 업데이트됨.

- [ ] **Step 10-5: 최종 커밋**

```bash
git add package.json src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/tauri.conf.json
git commit -m "chore: bump version to v0.4.4"
```

---

## 완료 기준 (Definition of Done)

- `npm run build` 통과 (TypeScript 에러 0)
- 모든 task의 수동 검증 체크리스트 통과
- 세션 생성 → PDF 첨부 → 이미지 생성 end-to-end 성공
- 생성 이미지가 `~/Downloads/AI_Gen/{세션명}/` 에 저장됨
- 버전 v0.4.4 커밋됨

## 위험 요소 및 대응

| 위험 | 대응 |
|------|------|
| PDF 이미지 추출 실패 (인코딩/CMYK 등) | try/catch로 감싸 텍스트 파싱은 유지, 이미지 0장 반환 |
| `tauri-plugin-opener` 권한 누락 | capabilities/default.json 업데이트 포함 |
| 기존 세션의 `autoSavePath` 참조로 인한 런타임 에러 | optional 필드로 유지하고 쓰기만 중단 (읽기는 무시) |
| 문서-only 케이스에서 빈 프롬프트 가드 누락 | Step 5-5의 기본 템플릿 주입으로 방어 |
| `DocumentManager`가 채팅 세션에서 레이아웃 깨짐 | 별도 컴팩트 props 또는 wrapping div 추가 (발견 시 Task 5에 추가) |
